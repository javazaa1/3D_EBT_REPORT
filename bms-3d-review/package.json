{
  "name": "bms-3d-review",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "db:init:local": "wrangler d1 execute bms-review --local --file=schema.sql",
    "db:init": "wrangler d1 execute bms-review --remote --file=schema.sql",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": { "wrangler": "^4.0.0" }
}
